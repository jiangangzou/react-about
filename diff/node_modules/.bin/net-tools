#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../net-tools/bin/net-tools" "$@"
  ret=$?
else 
  node  "$basedir/../net-tools/bin/net-tools" "$@"
  ret=$?
fi
exit $ret
